# mobile-phone-application-usin-stack
when we open multiple screens in a mobile app, each screen is pushed onto the stack. When you press the back button, the last opened screen is popped from the stack and the previous screen is displayed.
